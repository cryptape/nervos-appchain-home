import React from 'react'

const Loading = (props) => {
  const {} = props
  return (
    <div
      style={{
        backgroundImage: 'linear-gradient(to bottom, #36416c, #414c7b)',
        height: '100vh',
      }}
    >
      loading
    </div>
  )
}

export default Loading